// src/routes/accountingRoutes.jsx

/**
 * ======================================================
 * DEPRECATED ROUTE CONFIG (LEGACY)
 * ------------------------------------------------------
 * This project now uses src/routes/AppRoutes.jsx as the
 * single routing source of truth (React Router <Routes>).
 *
 * This file is kept only to avoid breaking imports if any
 * old code still references it.
 *
 * If you find it's unused, you may delete it later.
 * ======================================================
 */

export default [];
